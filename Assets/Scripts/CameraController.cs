using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour {

	//Doesn't need to move for now, but keeping this in case I decide it should move
}
